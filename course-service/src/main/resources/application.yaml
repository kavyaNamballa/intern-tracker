spring:
  application:
    name: course-service
  datasource:
    password: iiitn123
    url: jdbc:mysql://localhost:3306/sample
    username: root
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    serverUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost
    prefer-ip-address: true

server:
  port: 8083
